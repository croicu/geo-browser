{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Area Manifest",
  "type": "object",
  "required": ["version", "layers"],
  "additionalProperties": false,
  "properties": {
    "version": {
      "type": "integer",
      "description": "Manifest format version"
    },
    "layers": {
      "type": "array",
      "items": { "$ref": "#/definitions/layer" }
    },
    "aggregation": {
      "type": "object"
    },
    "deduping": {
      "type": "object"
    }
  },
  "definitions": {
    "nullableString": {
      "oneOf": [{ "type": "string" }, { "type": "null" }]
    },
    "layerStyle": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "type":        { "type": "string" },
        "color":       { "type": "string", "description": "CSS color string, e.g. \"#ff0000\"" },
        "opacity":     { "type": "number", "minimum": 0, "maximum": 1 },
        "radius":      { "type": "number", "minimum": 0 },
        "blur":        { "type": "number", "minimum": 0 },
        "radiusScale": { "type": "number", "minimum": 0 },
        "minRadius":   { "type": "number", "minimum": 0 },
        "maxRadius":   { "type": "number", "minimum": 0 },
        "strokeColor": { "type": "string", "description": "CSS color string" },
        "strokeWidth": { "type": "number", "minimum": 0 },
        "surface":     { "type": "boolean" },
        "minZoom":     { "type": "number", "minimum": 0 }
      }
    },
    "layerAcquisition": {
      "type": "object",
      "additionalProperties": false,
      "required": ["provider", "filters"],
      "properties": {
        "provider": { "type": "string" },
        "filters": {
          "type": "object",
          "additionalProperties": {
            "type": "array",
            "items": { "type": "string" }
          }
        }
      }
    },
    "layerGeometry": {
      "type": "object",
      "additionalProperties": false,
      "description": "__void__ only (bare entry). Affects the computed shape, not presentation — kept separate from style for that reason.",
      "properties": {
        "radius": {
          "type": "number",
          "minimum": 0,
          "description": "Exclusion radius in meters for source points without their own radius_m. Applies to every __void__* variant computed for the area."
        }
      }
    },
    "layer": {
      "type": "object",
      "required": ["id", "type", "visible"],
      "additionalProperties": false,
      "properties": {
        "id":          { "type": "string" },
        "type":        { "type": "string", "enum": ["heatmap", "circle", "__poi__", "__void__", "__user__", "__search__"] },
        "url":         { "$ref": "#/definitions/nullableString" },
        "visible":     { "type": "boolean" },
        "name":        { "type": "string" },
        "style":       { "$ref": "#/definitions/layerStyle" },
        "geometry":    { "$ref": "#/definitions/layerGeometry" },
        "acquisition": { "$ref": "#/definitions/layerAcquisition" }
      }
    }
  }
}
